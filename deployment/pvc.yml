apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: beam-mp-pvc
  namespace: beam-mp

spec:
  resources:
    requests:
      storage: 2Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce