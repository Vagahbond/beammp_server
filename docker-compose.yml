version: "3"

networks:
    back:

services:
  server:
    build:
      context: .
      target: build
    container_name: soundhive-front
    ports:
      - "8083:8083"
      - "30814:31853"
      - "8082:8082"
    volumes:
      - ./beamMP-data:/server_files/
      - ./:/src/app/
    environment:
      - ROCKET_PROFILE=debug
